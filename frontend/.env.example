# Адрес FastAPI-бэкенда. Браузер его не знает — ходит только в BFF SvelteKit.
RAGKB_BACKEND_URL=http://127.0.0.1:8000

# Локальный запуск без Angie: под каким логином ходить в бэкенд.
# В общем контуре не задавать — личность приходит заголовком от прокси.
RAGKB_DEV_USER=dev
# RAGKB_DEV_GROUPS=ragkb-admins
