# План реализации: идентификация пользователей

> **Для агентов:** ОБЯЗАТЕЛЬНЫЙ ПОДНАВЫК — используйте
> superpowers:subagent-driven-development (рекомендуется) либо
> superpowers:executing-plans, чтобы выполнять план задача за задачей.
> Шаги размечены чекбоксами (`- [ ]`) для отслеживания.

**Цель:** сервис узнаёт пользователя по заголовку от oauth2-proxy, закрывает
эндпоинты чтения аутентификацией, `/reindex` — членством в административной
группе, и перестаёт раскрывать состав индекса через `/health`.

**Архитектура:** аутентификацию ведёт oauth2-proxy перед сервисом, наверх
приходит заголовок с логином. В сервисе появляется шов `ragkb/auth.py`:
`User` и зависимости FastAPI, добывающие его из заголовков. Остальной код знает
только про `User` — способ его получения инкапсулирован в одном модуле.

**Технологии:** Python 3.10+, FastAPI, Starlette, стандартная библиотека.
Новых зависимостей не добавляется.

**Спека:** `docs/superpowers/specs/2026-08-19-identity-and-history-design.md`

## Общие ограничения

- **Новых зависимостей в `pyproject.toml` не добавлять.** Всё делается на том,
  что уже есть.
- **Версия oauth2-proxy не ниже 7.15.2** (CVE-2026-40575, CVSS 9.1). В образах
  фиксировать тег явно, не `latest`.
- **Версия MariaDB — `mariadb:11.8`**, не `latest`.
- **`auth.mode` по умолчанию `proxy`.** Отказ по умолчанию: забытая настройка
  даёт 401, а не открытый сервис.
- **Заголовок логина — `X-Forwarded-Preferred-Username`**, групп —
  `X-Forwarded-Groups`, почты — `X-Forwarded-Email`. Не `X-Auth-Request-*`:
  те выставляются в заголовки ответа для режима nginx `auth_request`
  и до сервиса в нашей топологии не доходят.
- **Тесты запускаются без pytest.** Файл `tests/test_*.py` исполняется как
  `python tests/test_auth.py`; в конце файла — раннер, перебирающий `globals()`.
  Тестовые функции обязаны быть без обязательных аргументов.
- **Язык кода и комментариев — русский**, как во всём проекте.
- **Коммит после каждой задачи.**

**Оснастка проверена заранее** на этом окружении (Python 3.13, Starlette 1.6.0,
FastAPI): `Headers(raw=...)` строится, `.getlist()` отдаёт `['a','b']` для
повторяющихся заголовков и `[]` для отсутствующих, `.get()` отдаёт `None`,
зависимость FastAPI выдаёт 401 **до вызова тела обработчика** — поэтому тестам
идентификации не нужен рабочий индекс. Имена заголовков обязаны быть ASCII
(`Headers.get` кодирует ключ в latin-1); имена групп могут быть любыми, они
сравниваются как строки.

При импорте `fastapi.testclient` выводится `StarletteDeprecationWarning`
про httpx — это шум окружения, а не признак поломки.

---

### Задача 0: Проверить заголовки эмпирически

Спека объявляет эту проверку условием начала работы. Причина: при
`auth.mode: proxy` ошибка в имени заголовка даёт 401 на каждый запрос, и
выяснится это только после того, как весь код написан.

**Файлы:**
- Создать: `docs/superpowers/plans/2026-08-19-identity-headers-observed.md`

**Интерфейсы:**
- Производит: подтверждённые имена заголовков и формат групп для задач 1–2.
  Если наблюдение разойдётся со спекой — правится спека, затем план, и только
  потом пишется код.

- [ ] **Шаг 1: Поднять одноразовый контур**

Создать `/tmp/hdrcheck/docker-compose.yml`. Keycloak здесь в dev-режиме —
это допустимо, потому что контур одноразовый и служит измерению, а не
эксплуатации.

```yaml
services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.4
    command: start-dev
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"

  echo:
    image: mendhak/http-https-echo:37
    environment:
      HTTP_PORT: 8081

  proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.15.2
    depends_on: [keycloak, echo]
    ports:
      - "4180:4180"
    command:
      - --provider=keycloak-oidc
      - --oidc-issuer-url=http://keycloak:8080/realms/master
      - --client-id=hdrcheck
      - --client-secret=ЗАМЕНИТЬ_НА_СЕКРЕТ_ИЗ_KEYCLOAK
      - --cookie-secret=ЗАМЕНИТЬ_НА_32_БАЙТА_BASE64
      - --email-domain=*
      - --http-address=0.0.0.0:4180
      - --upstream=http://echo:8081
      - --redirect-url=http://localhost:4180/oauth2/callback
      - --reverse-proxy=true
```

- [ ] **Шаг 2: Завести клиента в Keycloak**

Открыть `http://localhost:8080`, войти как `admin/admin`, в реалме `master`
создать клиента `hdrcheck`: тип confidential, `Valid redirect URIs` —
`http://localhost:4180/oauth2/callback`. Скопировать секрет в compose.

Секрет куки сгенерировать:

```bash
python3 -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
```

Завести пользователя с паролем и добавить его в две группы, например
`ragkb-admins` и `hr` — чтобы увидеть формат передачи нескольких групп.
В настройках клиента добавить mapper типа `Group Membership` с именем claim
`groups`, снять галку `Full group path`.

- [ ] **Шаг 3: Снять фактические заголовки**

Открыть `http://localhost:4180/`, войти. Эхо-сервер вернёт JSON со всеми
заголовками, которые до него дошли.

- [ ] **Шаг 4: Записать наблюдение**

Создать `docs/superpowers/plans/2026-08-19-identity-headers-observed.md`
и внести туда:

```markdown
# Наблюдение: заголовки от oauth2-proxy

Дата: <дата>
Версия oauth2-proxy: v7.15.2
Провайдер: keycloak-oidc

## Заголовки, дошедшие до upstream

<вставить релевантные строки из ответа эхо-сервера>

## Выводы

- Имя пользователя приходит в заголовке: <имя заголовка>
- Группы приходят: <одним заголовком через запятую | повторяющимися заголовками>
- Расхождения со спекой: <нет | перечислить>
```

- [ ] **Шаг 5: Сверить со спекой**

Если имена заголовков или формат групп отличаются от записанных в спеке —
остановиться, поправить спеку и этот план, и только потом идти дальше.
Если совпадают — продолжать.

- [ ] **Шаг 6: Погасить контур и закоммитить наблюдение**

```bash
cd /tmp/hdrcheck && docker compose down -v
cd /Users/adam977/repos/ragkb
git add docs/superpowers/plans/2026-08-19-identity-headers-observed.md
git commit -m "Наблюдение: фактические заголовки от oauth2-proxy"
```

---

### Задача 1: `User` и разбор заголовков

**Файлы:**
- Создать: `ragkb/auth.py`
- Создать: `tests/test_auth.py`

**Интерфейсы:**
- Потребляет: ничего.
- Производит:
  - `User(name: str, email: str = "", groups: tuple[str, ...] = ())`
    с методом `in_group(group: str) -> bool`
  - `parse_groups(values: Iterable[str]) -> tuple[str, ...]`
  - `ANONYMOUS: str` — имя пользователя в режиме `disabled`

- [ ] **Шаг 1: Написать падающий тест**

Создать `tests/test_auth.py`:

```python
"""Тесты идентификации. Запуск: python tests/test_auth.py"""
from __future__ import annotations

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from ragkb.auth import ANONYMOUS, User, parse_groups


# ------------------------------------------------------------- разбор групп

def test_parse_groups_splits_comma_separated():
    assert parse_groups(["ragkb-admins, hr ,legal"]) == ("ragkb-admins", "hr", "legal")


def test_parse_groups_handles_repeated_headers():
    assert parse_groups(["ragkb-admins", "hr"]) == ("ragkb-admins", "hr")


def test_parse_groups_drops_empty_and_duplicates():
    assert parse_groups(["hr,,hr", "   ", "legal"]) == ("hr", "legal")


def test_parse_groups_on_empty_input():
    assert parse_groups([]) == ()


# -------------------------------------------------------------------- User

def test_user_in_group():
    user = User(name="ivanov", groups=("hr", "ragkb-admins"))
    assert user.in_group("ragkb-admins")
    assert not user.in_group("legal")


def test_user_defaults_have_no_groups():
    assert User(name="ivanov").groups == ()


def test_anonymous_name_is_defined():
    assert ANONYMOUS == "anonymous"


if __name__ == "__main__":
    failed = 0
    for name, fn in sorted(globals().items()):
        if name.startswith("test_") and callable(fn):
            try:
                fn()
                print(f"  ok   {name}")
            except Exception as exc:
                failed += 1
                print(f"  FAIL {name}: {type(exc).__name__}: {exc}")
    print(f"\n{'все тесты пройдены' if not failed else f'провалов: {failed}'}")
    raise SystemExit(1 if failed else 0)
```

- [ ] **Шаг 2: Убедиться, что тест падает**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: `ModuleNotFoundError: No module named 'ragkb.auth'`

- [ ] **Шаг 3: Написать минимальную реализацию**

Создать `ragkb/auth.py`:

```python
"""Идентификация пользователя.

Аутентификацию ведёт oauth2-proxy перед сервисом; сюда приходит уже готовый
заголовок с логином. Весь остальной код знает только про User — если однажды
прокси сменится на другой механизм, правится только этот модуль.
"""
from __future__ import annotations

from dataclasses import dataclass
from typing import Iterable

# Имя пользователя, под которым работает сервис при выключенной аутентификации.
ANONYMOUS = "anonymous"


@dataclass(frozen=True)
class User:
    name: str
    email: str = ""
    groups: tuple[str, ...] = ()

    def in_group(self, group: str) -> bool:
        return group in self.groups


def parse_groups(values: Iterable[str]) -> tuple[str, ...]:
    """Разбирает группы из заголовков.

    Несколько групп приходят либо повторяющимися заголовками, либо одной
    строкой через запятую — формат зависит от настройки прокси. Разбираем оба
    случая, иначе проверка административной группы окажется ненадёжной.
    Порядок сохраняем, повторы отбрасываем.
    """
    groups: list[str] = []
    for value in values:
        for part in value.split(","):
            part = part.strip()
            if part and part not in groups:
                groups.append(part)
    return tuple(groups)
```

- [ ] **Шаг 4: Убедиться, что тесты проходят**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: 7 строк `ok`, затем `все тесты пройдены`.

- [ ] **Шаг 5: Коммит**

```bash
git add ragkb/auth.py tests/test_auth.py
git commit -m "Идентификация: User и разбор групп из заголовков"
```

---

### Задача 2: Настройки и зависимости FastAPI

**Файлы:**
- Изменить: `ragkb/config.py` (добавить `AuthConfig`, поле в `Config`, разбор секции, переменные окружения)
- Изменить: `ragkb/auth.py` (добавить `user_from_headers`, `current_user`, `optional_user`, `require_admin`)
- Изменить: `tests/test_auth.py` (дописать тесты)
- Изменить: `config.yaml` (секция `auth`)

**Интерфейсы:**
- Потребляет: `User`, `parse_groups`, `ANONYMOUS` из задачи 1.
- Производит:
  - `AuthConfig(mode, header, email_header, groups_header, admin_group)`
  - `Config.auth: AuthConfig`
  - `user_from_headers(headers: Headers, cfg: AuthConfig) -> User | None`
  - `current_user(request: Request) -> User` — 401, если не установлен
  - `optional_user(request: Request) -> User | None` — без исключения
  - `require_admin(request: Request) -> User` — 403 без нужной группы
  - Все три зависимости читают конфигурацию из `request.app.state.auth`

- [ ] **Шаг 1: Написать падающие тесты**

Дописать в `tests/test_auth.py` **перед** блоком `if __name__ == "__main__":`:

```python
# --------------------------------------------------- разбор заголовков

from starlette.datastructures import Headers

from ragkb.auth import current_user, optional_user, require_admin, user_from_headers
from ragkb.config import AuthConfig


def _headers(**pairs) -> Headers:
    return Headers(raw=[(k.lower().replace("_", "-").encode(), v.encode())
                        for k, v in pairs.items()])


def test_user_from_headers_reads_configured_header():
    headers = Headers(raw=[
        (b"x-forwarded-preferred-username", b"ivanov"),
        (b"x-forwarded-email", b"ivanov@example.com"),
        (b"x-forwarded-groups", b"ragkb-admins,hr"),
    ])
    user = user_from_headers(headers, AuthConfig())
    assert user is not None
    assert user.name == "ivanov"
    assert user.email == "ivanov@example.com"
    assert user.in_group("ragkb-admins")


def test_user_from_headers_without_header_returns_none():
    assert user_from_headers(Headers(raw=[]), AuthConfig()) is None


def test_user_from_headers_ignores_blank_name():
    headers = Headers(raw=[(b"x-forwarded-preferred-username", b"   ")])
    assert user_from_headers(headers, AuthConfig()) is None


def test_user_from_headers_respects_custom_header_name():
    cfg = AuthConfig(header="X-My-User")
    headers = Headers(raw=[(b"x-my-user", b"petrov")])
    user = user_from_headers(headers, cfg)
    assert user is not None and user.name == "petrov"


# ------------------------------------------------------------ зависимости

class _FakeRequest:
    """Минимальная замена Request: зависимостям нужны только headers и app.state."""

    class _State:
        def __init__(self, auth): self.auth = auth

    class _App:
        def __init__(self, auth): self.state = _FakeRequest._State(auth)

    def __init__(self, cfg: AuthConfig, headers: Headers | None = None):
        self.headers = headers if headers is not None else Headers(raw=[])
        self.app = _FakeRequest._App(cfg)


def test_current_user_raises_401_without_header():
    from fastapi import HTTPException
    try:
        current_user(_FakeRequest(AuthConfig()))
    except HTTPException as exc:
        assert exc.status_code == 401
    else:
        raise AssertionError("ожидался 401")


def test_current_user_returns_anonymous_when_disabled():
    user = current_user(_FakeRequest(AuthConfig(mode="disabled")))
    assert user.name == ANONYMOUS


def test_optional_user_returns_none_instead_of_raising():
    assert optional_user(_FakeRequest(AuthConfig())) is None


def test_require_admin_raises_403_without_group():
    from fastapi import HTTPException
    headers = Headers(raw=[(b"x-forwarded-preferred-username", b"ivanov"),
                           (b"x-forwarded-groups", b"hr")])
    try:
        require_admin(_FakeRequest(AuthConfig(), headers))
    except HTTPException as exc:
        assert exc.status_code == 403
    else:
        raise AssertionError("ожидался 403")


def test_require_admin_passes_with_group():
    headers = Headers(raw=[(b"x-forwarded-preferred-username", b"ivanov"),
                           (b"x-forwarded-groups", b"ragkb-admins")])
    user = require_admin(_FakeRequest(AuthConfig(), headers))
    assert user.name == "ivanov"


def test_auth_config_defaults_to_proxy_mode():
    assert AuthConfig().mode == "proxy"


def test_config_exposes_auth_section():
    from ragkb.config import Config
    cfg = Config.from_dict({"auth": {"mode": "disabled", "admin_group": "боссы"}})
    assert cfg.auth.mode == "disabled"
    assert cfg.auth.admin_group == "боссы"
```

- [ ] **Шаг 2: Убедиться, что тесты падают**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: `ImportError: cannot import name 'current_user' from 'ragkb.auth'`

- [ ] **Шаг 3: Добавить `AuthConfig` в `ragkb/config.py`**

Вставить после класса `LLMConfig`:

```python
@dataclass
class AuthConfig:
    # mode: "proxy" — логин приходит заголовком от oauth2-proxy
    #       "disabled" — аутентификации нет, всё работает от имени anonymous
    # По умолчанию закрыто: забытая настройка должна давать отказ,
    # а не открытый наружу сервис.
    mode: str = "proxy"
    # Заголовки семейства X-Forwarded-*, которые oauth2-proxy передаёт наверх
    # опцией --pass-user-headers. НЕ X-Auth-Request-*: те выставляются
    # в заголовки ответа для режима nginx auth_request.
    header: str = "X-Forwarded-Preferred-Username"
    email_header: str = "X-Forwarded-Email"
    groups_header: str = "X-Forwarded-Groups"
    admin_group: str = "ragkb-admins"
```

В классе `Config` добавить поле после `llm`:

```python
    auth: AuthConfig = field(default_factory=AuthConfig)
```

В `Config.from_dict` дополнить словарь `sections`:

```python
            "auth": AuthConfig,
```

В `Config._apply_env` дополнить `mapping`:

```python
            "RAGKB_AUTH_MODE": ("mode", self.auth),
            "RAGKB_AUTH_HEADER": ("header", self.auth),
            "RAGKB_AUTH_ADMIN_GROUP": ("admin_group", self.auth),
```

- [ ] **Шаг 4: Дописать `ragkb/auth.py`**

Добавить импорты в начало файла:

```python
from fastapi import HTTPException, Request
from starlette.datastructures import Headers

from .config import AuthConfig
```

Добавить в конец файла:

```python
def user_from_headers(headers: Headers, cfg: AuthConfig) -> User | None:
    """Собирает User из заголовков. None — пользователь не установлен."""
    name = (headers.get(cfg.header) or "").strip()
    if not name:
        return None
    return User(
        name=name,
        email=(headers.get(cfg.email_header) or "").strip(),
        groups=parse_groups(headers.getlist(cfg.groups_header)),
    )


def current_user(request: Request) -> User:
    """Зависимость FastAPI: кто выполняет запрос. 401, если неизвестно."""
    cfg: AuthConfig = request.app.state.auth
    if cfg.mode == "disabled":
        return User(name=ANONYMOUS)
    user = user_from_headers(request.headers, cfg)
    if user is None:
        raise HTTPException(status_code=401, detail="Не аутентифицирован")
    return user


def optional_user(request: Request) -> User | None:
    """То же, но без исключения — для эндпоинтов, открытых без аутентификации."""
    try:
        return current_user(request)
    except HTTPException:
        return None


def require_admin(request: Request) -> User:
    """Зависимость для административных операций."""
    cfg: AuthConfig = request.app.state.auth
    user = current_user(request)
    if cfg.mode == "disabled":
        return user
    if not user.in_group(cfg.admin_group):
        raise HTTPException(
            status_code=403,
            detail=f"Требуется членство в группе «{cfg.admin_group}»",
        )
    return user
```

- [ ] **Шаг 5: Убедиться, что тесты проходят**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: 18 строк `ok`, затем `все тесты пройдены`.

- [ ] **Шаг 6: Проверить, что старые тесты не сломались**

```bash
.venv/bin/python tests/test_pipeline.py
```

Ожидается: `все тесты пройдены` (29 тестов).

- [ ] **Шаг 7: Добавить секцию в `config.yaml`**

Дописать в конец файла:

```yaml
auth:
  # proxy — логин приходит заголовком от oauth2-proxy (боевой режим)
  # disabled — аутентификации нет, всё работает от имени anonymous
  mode: proxy
  header: X-Forwarded-Preferred-Username
  email_header: X-Forwarded-Email
  groups_header: X-Forwarded-Groups
  admin_group: ragkb-admins
```

- [ ] **Шаг 8: Коммит**

```bash
git add ragkb/auth.py ragkb/config.py tests/test_auth.py config.yaml
git commit -m "Идентификация: настройки и зависимости FastAPI"
```

---

### Задача 3: Закрыть эндпоинты чтения

**Файлы:**
- Изменить: `ragkb/api.py` (сохранить конфигурацию в `app.state`, навесить зависимость на `/`, `/ask`, `/ask/stream`, `/search`)
- Изменить: `tests/test_auth.py` (тесты через `TestClient`)

**Интерфейсы:**
- Потребляет: `current_user`, `ANONYMOUS`, `AuthConfig` из задач 1–2.
- Производит: `create_app(cfg)` с заполненным `app.state.auth`; эндпоинты
  чтения возвращают 401 без заголовка при `auth.mode: proxy`.

- [ ] **Шаг 1: Написать падающие тесты**

Дописать в `tests/test_auth.py` перед раннером:

```python
# ------------------------------------------------------- эндпоинты чтения

def _client(mode: str = "proxy"):
    """Приложение поверх заведомо отсутствующего индекса.

    index_dir указывает в несуществующий каталог намеренно: у Config значение
    по умолчанию — «data/index», а он в репозитории есть, и тогда часть проверок
    пошла бы не по той ветке. Обработчиков это не касается — зависимости
    идентификации отрабатывают раньше, до обращения к пайплайну.
    """
    import tempfile

    from fastapi.testclient import TestClient

    from ragkb.api import create_app
    from ragkb.config import Config

    cfg = Config()
    cfg.index_dir = str(Path(tempfile.gettempdir()) / "ragkb-нет-такого-индекса")
    cfg.auth.mode = mode
    return TestClient(create_app(cfg), raise_server_exceptions=False)


def test_ask_requires_authentication():
    resp = _client().post("/ask", json={"question": "тест"})
    assert resp.status_code == 401, resp.status_code


def test_search_requires_authentication():
    resp = _client().post("/search", json={"query": "тест"})
    assert resp.status_code == 401, resp.status_code


def test_ask_stream_requires_authentication():
    resp = _client().post("/ask/stream", json={"question": "тест"})
    assert resp.status_code == 401, resp.status_code


def test_index_page_requires_authentication():
    assert _client().get("/").status_code == 401


def test_index_page_open_when_auth_disabled():
    assert _client("disabled").get("/").status_code == 200
```

- [ ] **Шаг 2: Убедиться, что тесты падают**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: пять `FAIL` с кодом 503 или 200 вместо 401 — зависимость ещё
не навешена.

- [ ] **Шаг 3: Сохранить конфигурацию в состоянии приложения**

В `ragkb/api.py` добавить импорты:

```python
from fastapi import Depends, FastAPI, HTTPException

from .auth import User, current_user, optional_user, require_admin
```

В `create_app`, сразу после создания `app`:

```python
    # Зависимости идентификации читают настройки отсюда.
    app.state.auth = cfg.auth
    if cfg.auth.mode == "disabled":
        print(
            "ВНИМАНИЕ: аутентификация выключена (auth.mode: disabled). "
            "Все запросы выполняются от имени «anonymous». "
            "В общем контуре так работать нельзя."
        )
```

- [ ] **Шаг 4: Навесить зависимость на эндпоинты чтения**

Изменить сигнатуры четырёх обработчиков в `ragkb/api.py`:

```python
    @app.post("/ask")
    def ask(req: AskRequest, user: User = Depends(current_user)) -> dict[str, Any]:
```

```python
    @app.post("/ask/stream")
    def ask_stream(req: AskRequest, user: User = Depends(current_user)) -> StreamingResponse:
```

```python
    @app.post("/search")
    def search(req: SearchRequest, user: User = Depends(current_user)) -> dict[str, Any]:
```

```python
    @app.get("/", response_class=HTMLResponse)
    def index_page(user: User = Depends(current_user)) -> str:
```

Тела обработчиков не меняются: `user` пока не используется, он появится
в части 2 при привязке диалогов.

- [ ] **Шаг 5: Убедиться, что тесты проходят**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: 23 строки `ok`.

- [ ] **Шаг 6: Коммит**

```bash
git add ragkb/api.py tests/test_auth.py
git commit -m "Идентификация: эндпоинты чтения требуют аутентификации"
```

---

### Задача 4: `/health` и `/reindex`

Здесь чинятся два дефекта, найденные при разметке доступа.

**Файлы:**
- Изменить: `ragkb/api.py:40-46` (`/health`), `ragkb/api.py:72-81` (`/reindex`)
- Изменить: `tests/test_auth.py`

**Интерфейсы:**
- Потребляет: `optional_user`, `require_admin` из задачи 2.
- Производит: `/health` без аутентификации отдаёт только поле `status`;
  `/reindex` требует членства в административной группе.

**Уточнение к спеке.** Спека предписывает неаутентифицированному `/health`
отвечать `{"status": "ok"}`. Так `HEALTHCHECK` перестал бы отличать рабочий
сервис от сломанного. Статус отдаём честно (`ok` либо `no_index`), но без
числа документов, имени эмбеддера и пути к индексу — утечки нет, проверка
живости работает.

- [ ] **Шаг 1: Написать падающие тесты**

Дописать в `tests/test_auth.py` перед раннером:

```python
# --------------------------------------------------- health и reindex

def test_health_open_without_authentication():
    assert _client().get("/health").status_code == 200


def test_health_hides_index_internals_from_anonymous():
    body = _client().get("/health").json()
    assert set(body) == {"status"}, body


def test_health_status_is_truthful_without_index():
    assert _client().get("/health").json()["status"] == "no_index"


def test_reindex_forbidden_without_admin_group():
    headers = {"X-Forwarded-Preferred-Username": "ivanov",
               "X-Forwarded-Groups": "hr"}
    resp = _client().post("/reindex", headers=headers)
    assert resp.status_code == 403, resp.status_code


def test_reindex_unauthenticated_gives_401():
    assert _client().post("/reindex").status_code == 401
```

- [ ] **Шаг 2: Убедиться, что тесты падают**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: `FAIL` на всех пяти — `/health` пока отдаёт `detail`, `/reindex`
пускает кого угодно.

- [ ] **Шаг 3: Урезать `/health`**

Заменить обработчик в `ragkb/api.py`:

```python
    @app.get("/health")
    def health(user: User | None = Depends(optional_user)) -> dict[str, Any]:
        # Без аутентификации отдаём только статус: HEALTHCHECK должен работать,
        # но состав индекса, имя эмбеддера и пути наружу не уходят.
        try:
            stats = pipeline().stats()
        except HTTPException as exc:
            return {"status": "no_index"} if user is None else {
                "status": "no_index",
                "detail": exc.detail,
            }
        return {"status": "ok"} if user is None else {"status": "ok", **stats}
```

- [ ] **Шаг 4: Закрыть `/reindex`**

Заменить сигнатуру обработчика в `ragkb/api.py`:

```python
    @app.post("/reindex")
    def reindex(user: User = Depends(require_admin)) -> dict[str, Any]:
```

Тело не меняется.

- [ ] **Шаг 5: Убедиться, что тесты проходят**

```bash
.venv/bin/python tests/test_auth.py
```

Ожидается: 28 строк `ok`.

- [ ] **Шаг 6: Проверить, что HEALTHCHECK по-прежнему проходит**

```bash
.venv/bin/python -c "
from fastapi.testclient import TestClient
from ragkb.api import create_app
from ragkb.config import Config
r = TestClient(create_app(Config())).get('/health')
print(r.status_code, r.json())
assert r.status_code == 200
"
```

Ожидается: `200 {'status': 'no_index'}` — код 200, значит `curl -fsS`
в `HEALTHCHECK` не упадёт.

- [ ] **Шаг 7: Коммит**

```bash
git add ragkb/api.py tests/test_auth.py
git commit -m "Идентификация: /reindex под административную группу, /health урезан"
```

---

### Задача 5: Выход из системы и документация

**Файлы:**
- Изменить: `ragkb/api.py` (константа `UI_HTML`: ссылка выхода и имя пользователя)
- Изменить: `README.md` (раздел «Быстрый старт», раздел «Ограничения»)

**Интерфейсы:**
- Потребляет: `/health` из задачи 4 — страница уже запрашивает его при загрузке.
- Производит: работающая смена пользователя; документация, соответствующая коду.

- [ ] **Шаг 1: Добавить ссылку выхода в интерфейс**

В `ragkb/api.py`, в константе `UI_HTML`, заменить блок заголовка:

```html
  <h1>База знаний</h1>
  <div class="sub" id="status">загрузка…</div>
```

на:

```html
  <h1>База знаний</h1>
  <div class="sub" id="status">загрузка…</div>
  <div class="sub"><a href="/oauth2/sign_out" id="signout">выйти</a></div>
```

- [ ] **Шаг 2: Проверить вручную**

```bash
RAGKB_AUTH_MODE=disabled .venv/bin/python -m ragkb.cli serve --port 8010
```

Открыть `http://127.0.0.1:8010/`, убедиться, что ссылка «выйти» видна.
Остановить сервер по Ctrl-C.

Ссылка ведёт на `/oauth2/sign_out` — эндпоинт самого oauth2-proxy, поэтому
без прокси она даст 404. Это ожидаемо.

- [ ] **Шаг 3: Поправить «Быстрый старт» в README**

В `README.md` заменить блок быстрого старта на:

````markdown
```bash
uv sync                            # окружение из uv.lock

cp ваши_документы/* data/docs/
uv run ragkb index                 # построить индекс

# Аутентификация по умолчанию включена и ждёт заголовок от oauth2-proxy.
# Для локального запуска её отключают явно — в общем контуре так нельзя:
# все запросы пойдут от имени «anonymous».
export RAGKB_AUTH_MODE=disabled

uv run ragkb ask "За сколько дней подавать заявление на отпуск?"
uv run ragkb serve                 # веб-интерфейс на http://127.0.0.1:8000
```
````

- [ ] **Шаг 4: Обновить раздел «Ограничения»**

В `README.md` заменить пункт про права доступа:

```markdown
- **Разграничение доступа грубое.** Аутентификация внешняя (oauth2-proxy
  и Keycloak), доступ к сервису ограничивается группой Active Directory
  на стороне прокси. Разграничения по отдельным документам нет: метаданные
  в Chroma для этого есть (`doc_id`, `source`, `section`), не хватает фильтра
  `where` в запросе и модели прав.
```

- [ ] **Шаг 5: Убедиться, что тесты не сломались**

```bash
.venv/bin/python tests/test_auth.py && .venv/bin/python tests/test_pipeline.py
```

Ожидается: оба файла — `все тесты пройдены`.

- [ ] **Шаг 6: Коммит**

```bash
git add ragkb/api.py README.md
git commit -m "Идентификация: выход из системы и правка документации"
```

---

### Задача 6: Контур развёртывания

**Файлы:**
- Изменить: `docker-compose.yml` (убрать проброс портов у `rag`, добавить keycloak, mariadb, oauth2-proxy)
- Создать: `.env.example`

**Интерфейсы:**
- Потребляет: `auth.mode: proxy` по умолчанию из задачи 2.
- Производит: контур, в котором ragkb недоступен снаружи и виден только прокси.

- [ ] **Шаг 1: Убрать проброс портов у сервиса `rag`**

В `docker-compose.yml` удалить строки 30–31:

```yaml
    ports:
      - "8000:8000"
```

Сервис остаётся доступен по имени `rag` внутри сети compose — этого
достаточно, наружу его публиковать не нужно.

- [ ] **Шаг 2: Добавить компоненты идентификации**

Дописать в `docker-compose.yml` перед секцией `volumes`:

```yaml
  mariadb:
    image: mariadb:11.8
    environment:
      MARIADB_DATABASE: keycloak
      MARIADB_USER: keycloak
      MARIADB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      MARIADB_ROOT_PASSWORD: ${MARIADB_ROOT_PASSWORD}
    volumes:
      - keycloak_db:/var/lib/mysql

  keycloak:
    image: quay.io/keycloak/keycloak:26.4
    depends_on:
      - mariadb
    command: start --optimized
    environment:
      KC_DB: mariadb
      KC_DB_URL: jdbc:mariadb://mariadb:3306/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KC_HOSTNAME: ${KEYCLOAK_HOSTNAME}
      KC_HTTPS_CERTIFICATE_FILE: /certs/tls.crt
      KC_HTTPS_CERTIFICATE_KEY_FILE: /certs/tls.key
      KC_BOOTSTRAP_ADMIN_USERNAME: ${KEYCLOAK_ADMIN}
      KC_BOOTSTRAP_ADMIN_PASSWORD: ${KEYCLOAK_ADMIN_PASSWORD}
    volumes:
      - ./certs:/certs:ro

  proxy:
    # Версия закреплена: CVE-2026-40575 (обход аутентификации подделкой
    # X-Forwarded-Uri) затрагивает 7.5.0–7.15.1.
    image: quay.io/oauth2-proxy/oauth2-proxy:v7.15.2
    depends_on:
      - keycloak
      - rag
    ports:
      - "443:4180"
    volumes:
      - ./certs:/certs:ro
    command:
      - --provider=keycloak-oidc
      - --oidc-issuer-url=${KEYCLOAK_ISSUER_URL}
      - --client-id=ragkb
      - --client-secret=${OAUTH2_CLIENT_SECRET}
      - --cookie-secret=${OAUTH2_COOKIE_SECRET}
      - --cookie-secure=true
      - --email-domain=*
      - --http-address=
      - --https-address=0.0.0.0:4180
      - --tls-cert-file=/certs/tls.crt
      - --tls-key-file=/certs/tls.key
      - --upstream=http://rag:8000
      - --redirect-url=${OAUTH2_REDIRECT_URL}
      - --reverse-proxy=true
      - --allowed-group=/ragkb-users
      - --pass-user-headers=true
      - --skip-auth-strip-headers=true
```

В секцию `volumes` внизу файла добавить:

```yaml
  keycloak_db:
```

- [ ] **Шаг 3: Создать `.env.example`**

```bash
cat > .env.example <<'EOF'
# Скопируйте в .env и заполните. Файл .env в репозиторий не коммитится.

MARIADB_ROOT_PASSWORD=
KEYCLOAK_DB_PASSWORD=
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=
KEYCLOAK_HOSTNAME=kb.example.local
KEYCLOAK_ISSUER_URL=https://kb.example.local:8443/realms/company

OAUTH2_CLIENT_SECRET=
# Сгенерировать:
#   python3 -c "import base64,os;print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
OAUTH2_COOKIE_SECRET=
OAUTH2_REDIRECT_URL=https://kb.example.local/oauth2/callback

EOF
```

- [ ] **Шаг 4: Добавить `.env` в `.gitignore`**

```bash
printf '\n.env\ncerts/\n' >> .gitignore
```

- [ ] **Шаг 5: Проверить синтаксис compose**

```bash
cp .env.example .env
docker compose config > /dev/null && echo "синтаксис верен"
rm .env
```

Ожидается: `синтаксис верен`. Проверяется только разбор файла — поднимать
контур на этом шаге не нужно, для этого требуются сертификаты и настроенный
реалм Keycloak.

- [ ] **Шаг 6: Проверить, что сервис не публикуется наружу**

```bash
grep -A3 '^  rag:' docker-compose.yml | grep -q 'ports' \
  && echo "ОШИБКА: у rag остался проброс портов" \
  || echo "верно: rag наружу не публикуется"
```

Ожидается: `верно: rag наружу не публикуется`.

- [ ] **Шаг 7: Коммит**

```bash
git add docker-compose.yml .env.example .gitignore
git commit -m "Идентификация: контур развёртывания с Keycloak и oauth2-proxy"
```

---

## Что остаётся за пределами этого плана

**Часть 2 спеки — история диалогов.** Получит отдельный план после того, как
эта часть ляжет: `ragkb/history.py`, схема SQLite, эндпоинты `/conversations`,
срок хранения, атомарная отметка уборки.

**Настройка реалма Keycloak** — работа эксплуатации, не разработки: создание
реалма, федерация с Active Directory, клиент `ragkb`, группы `ragkb-users`
и `ragkb-admins`, mapper `Group Membership`.

**Выпуск сертификатов** для внутреннего контура — открытый вопрос № 6 спеки.

**Проверка развёртывания:** после поднятия контура убедиться, что порт 8000
не отвечает ни с хоста, ни из сети. Это условие, на котором держится защита
от подделки заголовка.
